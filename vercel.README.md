# Configuración Vercel (raíz del repo)

Solo usar este `vercel.json` si el proyecto en Vercel tiene **Root Directory vacío** (no recomendado).

La configuración canónica es **Root Directory = `web`** + `web/vercel.json`.

Producción: [https://arriendoseguro.vercel.app/](https://arriendoseguro.vercel.app/) — guía completa en `web/docs/checklist-firebase-vercel-operacion.md` §0.
