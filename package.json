{
  "name": "arriendoseguro",
  "private": true,
  "description": "Raíz del monorepo: la app Next.js vive en web/. Usado cuando Vercel tiene Root Directory en la raíz del repo.",
  "scripts": {
    "build": "npm run build --prefix web",
    "dev": "npm run dev --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  }
}
